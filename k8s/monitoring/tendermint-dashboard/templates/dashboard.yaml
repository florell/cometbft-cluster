apiVersion: v1
kind: ConfigMap
metadata:
  name: tendermint-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  tendermint.json: |-
{{ .Files.Get "dashboards/tendermint.json" | indent 4 }}
